#include <iostream>
using namespace std;

#include <climits>
#include <vector>
vector<int> arr;

void solve()
{
    int n, c, d;
    cin >> n >> c >> d;

    int minmum = INT_MAX;
    for (int i = 0; i < n * n; i++)
    {
        arr.push
    }
}
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        solve();
    }

    return 0;
}